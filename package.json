{
  "name": "krea-ai-mcp",
  "version": "0.1.0",
  "description": "Local MCP server for Krea image generation",
  "type": "module",
  "files": [
    "dist"
  ],
  "bin": {
    "krea-ai-mcp": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "dev": "tsx src/index.ts",
    "start": "node dist/index.js",
    "prepare": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/node": "^22.13.1",
    "tsx": "^4.20.5",
    "typescript": "^5.7.3"
  },
  "engines": {
    "node": ">=20"
  }
}
